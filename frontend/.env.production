# Empty = all API calls become relative (e.g. /api/foo), which Vercel
# then proxies to the Vultr backend via the rewrites in vercel.json.
# This sidesteps the HTTPS-over-HTTP mixed-content block.
VITE_API_URL=
